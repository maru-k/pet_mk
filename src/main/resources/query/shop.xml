<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="shop">
	<resultMap id="shopInfo" type="com.plat.pet.shop.model.Shop">
		<result column="SHOP_ID" 			property="shopId"/>
		<result column="USER_ID" 			property="userId"/>
		<result column="SHOP_NAME" 			property="shopNm"/>
		<result column="SHOP_STAT_CD" 		property="shopStatCd"/>
		<result column="SHOP_SECT_CD" 		property="shopSectCd"/>
		<result column="SHOP_DESC" 			property="shopDesc"/>
		<result column="SHOP_TELNO" 		property="shopTelNo"/>
		<result column="SHOP_CELNO" 		property="shopCelNo"/>
		<result column="SHOP_PHOTO_ID" 		property="shopPhotoId"/>
		<result column="SHOP_ADDR" 			property="shopAddr"/>
		<result column="SHOP_ADDR_DETAIL" 	property="shopAddrDetail"/>
		<result column="SHOP_POINT" 		property="shopPoint"/>
	</resultMap>

	<insert id="insertNewShop" parameterType="Shop">
		INSERT INTO TB_SHOP_F (
			  SHOP_ID
			, USER_ID
			, SHOP_NAME
			, SHOP_STAT_CD
			, SHOP_SECT_CD
			, SHOP_DESC
			, SHOP_TELNO
			, SHOP_CELNO
			, SHOP_PHOTO_ID
			, SHOP_ADDR
			, SHOP_ADDR_DETAIL
			, LAST_ACT_DTM
			, SHOP_POINT
			)
		VALUES(
			  #{shopId}
			, #{userId}
			, #{shopNm}
			, #{shopStatCd}
			, #{shopSectCd}
			, #{shopDesc}
			, #{shopTelNo}
			, #{shopCelNo}
			, #{shopPhotoId}
			, #{shopAddr}
			, #{shopAddrDetail}
			, now()
			, 0
			)
	</insert>
	<select id="selectShopInfo" parameterType="String" resultMap="shopInfo">
		SELECT
			  SHOP_ID		
			, USER_ID		
			, SHOP_NAME		
			, SHOP_STAT_CD	
			, SHOP_SECT_CD
			, SHOP_DESC
			, SHOP_TELNO
			, SHOP_CELNO
			, SHOP_PHOTO_ID
			, SHOP_ADDR
			, SHOP_ADDR_DETAIL
			, LAST_ACT_DTM
			, SHOP_POINT
		FROM TB_SHOP_F
		WHERE SHOP_ID = #{shopId}
	</select>
</mapper>